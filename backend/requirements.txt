fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# Local Aer simulator backend.
# qiskit-aer must be >=0.17 to work with Qiskit 2.x — older aer imports
# ProviderV1, which was removed in Qiskit 2.0.
qiskit>=2.0.0
qiskit-aer>=0.17.0

# Optional MongoDB Atlas mirror (see mongo_logger.py) — only used if
# MONGODB_URI is set in .env.
pymongo>=4.6.0
